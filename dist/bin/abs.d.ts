#!/usr/bin/env node
/**
 * abs - Audiobookshelf CLI
 *
 * Usage:
 *   abs library             List libraries
 *   abs books               List books
 *   abs search <query>      Search library
 *   abs play <id>           Start playback
 *   abs resume              Resume last book
 *   abs pause               Pause playback
 *   abs stop                Stop and sync
 *   abs devices             List Cast devices
 *   abs sleep <minutes>     Set sleep timer
 */
export {};
//# sourceMappingURL=abs.d.ts.map